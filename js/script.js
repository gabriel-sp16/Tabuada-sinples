let numeroTabuada = Number(prompt("Digite um numero para descobrir a tabuada:"));

for (let indice = 0; indice <= 10; indice++) {
    alert(`${numeroTabuada} X ${indice} = ${numeroTabuada * indice}`);
}